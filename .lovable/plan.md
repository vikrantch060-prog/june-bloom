Three small copy edits in `src/routes/index.tsx`:

1. **Empty-state message** — shorten to just "the first memory unlocks today." (remove "add a row to the archive to begin.").

2. **Mode label** ("lazy romance" / "cozy bookstore" / "stargazing") — remove the entire `<div>` above the H1 that renders the mode-based tagline.

3. **Footer line** — replace "tomorrow is locked. come back." with "Tomorrow is mystery, But Today is a Gift".

No other files affected.